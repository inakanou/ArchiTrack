# Railway deployment configuration for Frontend
# Root Directory should be set to /frontend in Railway UI

[build]
builder = "DOCKERFILE"
# GitHub Actionsから制御するため自動デプロイを無効化
watchPatterns = []

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
