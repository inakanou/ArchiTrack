#!/bin/sh
# ============================================================================
# Pre-commit Hook Wrapper
# ============================================================================
# このファイルは Git/Husky によって実行されます (sh 環境)
# 実際のロジックは scripts/pre-commit.sh (bash) で実装されています
#
# このアーキテクチャにより:
# - 統一されたパターン (pre-pushと同じ構造)
# - 将来的なbash機能追加が容易
# - スクリプトを独立してテスト可能
# - Gitでバージョン管理可能
# ============================================================================

# bashで実際のスクリプトを実行
exec bash "$(dirname "$0")/scripts/pre-commit.sh" "$@"
