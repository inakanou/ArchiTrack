echo "🔎 Running type checks before push..."

# Backend type check
if [ -d "backend" ]; then
  echo "🔍 Checking backend types..."
  npm --prefix backend run type-check
  if [ $? -ne 0 ]; then
    echo "❌ Backend type check failed. Push aborted."
    exit 1
  fi
fi

# Frontend type check
if [ -d "frontend" ]; then
  echo "🔍 Checking frontend types..."
  npm --prefix frontend run type-check
  if [ $? -ne 0 ]; then
    echo "❌ Frontend type check failed. Push aborted."
    exit 1
  fi
fi

# E2E type check
echo "🔍 Checking E2E types..."
npm run type-check
if [ $? -ne 0 ]; then
  echo "❌ E2E type check failed. Push aborted."
  exit 1
fi

# Backend unit tests
if [ -d "backend" ]; then
  echo "🧪 Running backend unit tests..."
  npm --prefix backend run test:unit
  if [ $? -ne 0 ]; then
    echo "❌ Backend unit tests failed. Push aborted."
    exit 1
  fi
fi

# Frontend unit tests
if [ -d "frontend" ]; then
  echo "🧪 Running frontend unit tests..."
  npm --prefix frontend run test
  if [ $? -ne 0 ]; then
    echo "❌ Frontend unit tests failed. Push aborted."
    exit 1
  fi
fi

# Backend integration tests - データベース接続が必要なためスキップ
# if [ -d "backend" ]; then
#   echo "🔗 Running backend integration tests..."
#   npm --prefix backend run test:integration
#   if [ $? -ne 0 ]; then
#     echo "❌ Backend integration tests failed. Push aborted."
#     exit 1
#   fi
# fi

# E2Eテストの実行 - ブラウザ環境が必要なためスキップ
# プッシュ前に時間がかかる場合はコメントアウトしてください
# echo "🧪 Running E2E tests..."
# npm run test:e2e
# if [ $? -ne 0 ]; then
#   echo "❌ E2E tests failed. Push aborted."
#   exit 1
# fi

echo "✅ All checks passed!"
