#!/bin/sh
# ============================================================================
# Pre-push Hook Wrapper
# ============================================================================
# このファイルは Git/Husky によって実行されます (sh 環境)
# 実際のロジックは scripts/pre-push.sh (bash) で実装されています
#
# このアーキテクチャにより:
# - Huskyの制約 (sh実行) を回避
# - bash固有機能 (pipefail等) を使用可能
# - スクリプトを独立してテスト可能
# - Gitでバージョン管理可能
# ============================================================================

# bashで実際のスクリプトを実行
exec bash "$(dirname "$0")/scripts/pre-push.sh" "$@"
