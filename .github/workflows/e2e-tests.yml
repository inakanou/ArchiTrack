name: E2E Tests

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

jobs:
  e2e-tests:
    name: Playwright E2E Tests
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright browsers
        run: npx playwright install --with-deps chromium

      - name: Run E2E code lint
        run: npm run lint

      - name: Run E2E code format check
        run: npm run format:check

      - name: Start Docker Compose services
        run: |
          docker compose up -d --build
          echo "Waiting for services to start..."
          sleep 15

      - name: Check Docker containers status
        run: docker compose ps

      - name: Wait for PostgreSQL to be ready
        run: |
          timeout 60 bash -c 'until docker compose exec -T postgres pg_isready -U postgres; do sleep 2; done'

      - name: Wait for Redis to be ready
        run: |
          timeout 60 bash -c 'until docker compose exec -T redis redis-cli ping; do sleep 2; done'

      - name: Wait for Backend to be ready
        run: |
          echo "Waiting for backend to start..."
          timeout 180 bash -c 'until curl -s http://localhost:3000/health > /dev/null 2>&1; do echo "Waiting for backend..."; sleep 5; done'
          echo "Backend is ready!"

      - name: Wait for Frontend to be ready
        run: |
          echo "Waiting for frontend to start..."
          timeout 180 bash -c 'until curl -s http://localhost:5173 > /dev/null 2>&1; do echo "Waiting for frontend..."; sleep 5; done'
          echo "Frontend is ready!"

      - name: Show Docker logs on failure
        if: failure()
        run: |
          echo "=== Docker Compose Services ==="
          docker compose ps
          echo "=== Backend Logs ==="
          docker compose logs backend
          echo "=== Frontend Logs ==="
          docker compose logs frontend

      - name: Run Playwright tests
        run: npm run test:e2e
        env:
          CI: true

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 30

      - name: Upload screenshots
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-screenshots
          path: playwright-report/screenshots/
          retention-days: 30

      - name: Stop Docker Compose services
        if: always()
        run: docker compose down
