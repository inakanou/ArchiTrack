# 見積書PDF出力 書式リファレンス

見積書PDF出力の目標書式。参照元は実案件の見積書PDF（`御見積書_JAあかし 農業倉庫改修工事_第2版.pdf`、8ページ、Producer: `Microsoft: Print To PDF`）。

参照PDFは一時領域に置かれた資料で永続しないため、実装に必要な寸法・配置をここに記録する。

## 座標系の読み方

参照PDFの各ページは `0.75 0 0 -0.75 0 595.32 cm` を適用しており、コンテンツは**96dpi ピクセル空間**（1122.56 × 793.76 px）で作られている。本書では以下2系統で表記する。

- **px**: 参照PDF内部の論理座標（左上原点、Y下向き）
- **mm**: 実寸（px × 0.264583）

フォントサイズも同様で、PDF内の `Tf` 値 × 0.75 が実際のポイント数になる。本書の pt はすべて実寸。

---

## 1. ページ設定

| 項目 | 値 |
|---|---|
| 用紙 | **A4 横（ランドスケープ）** 841.92 × 595.32 pt = 297 × 210 mm |
| 論理座標空間 | 1122.56 × 793.76 px |
| 総ページ数 | 表紙1 ＋ 内訳書1 ＋ 明細書N |
| ページ番号 | 全ページ通し。`Page.1` から始まる |

> 現行実装は `orientation: 'portrait'`（A4縦）。**横向きへの変更が必要。**

---

## 2. ページ構成

| ページ | 種別 | 内容 |
|---|---|---|
| 1 | **表紙** | 御見積書。宛先・見積金額・工事件名/場所・別途工事・自社情報 |
| 2 | **内訳書** | 第1階層項目の一覧＋値引行＋合計 |
| 3〜 | **明細書** | 第1階層ごとに改ページ。配下の子項目を列挙し、末尾に合計 |

### 明細書ページの生成ルール
- **子項目を持つ第1階層のみ**が明細書ページを持つ。
- 参照PDFでは内訳書に `Ａ`〜`Ｈ` ＋ `【値引】` の9行があるが、明細書は `Ａ`〜`Ｆ` の6ページのみ。`Ｇ.現場管理費` `Ｈ.一般管理費`（いずれも 式×1 の諸経費）と `【値引】` は子を持たないため明細書ページを作らない。
- 1ページに収まらない場合の継続処理は参照PDFに実例が無い（最大7明細行）。**要件定義で決める。**

---

## 3. 表紙（Page 1）

### 外枠
矩形 (69.36, 53.52) – (1064.24, 738.48) px = (18.4, 14.2) – (281.6, 195.4) mm、線幅 2.72px（= 0.72pt）

### 表題
- 文字列: `御 見 積 書`（1文字ずつ全角スペース区切り）
- 位置: x=470, y=163 px / **25pt**
- **背景に網掛け矩形**: (454.8, 128.24) – (667.44, 172.72) px、塗り `RGB(192,192,192)`（0.752941 グレー）、黒枠線あり

### 各要素の配置

| 要素 | x (px) | y (px) | サイズ | 内容例 |
|---|---|---|---|---|
| ページ番号 | 914 | 161 | 10pt | `Page.1` |
| 日付 | 814 | 220 | 10pt | `２０２６ 年５ 月１ 日`（全角数字） |
| 宛先（組織名） | 116 | 241 | **14pt** | `あかし農業協同組合` |
| 宛先（役職氏名） | 117 | 272 | **14pt** | `代表理事組合長 大西 弘訓` |
| 敬称 | 353 | 272 | 14pt | `様` |
| リード文 | 117 | 326 | 12pt | `下記のとおり御見積申し上げます。` |
| **見積金額** | 301 | 407 | **20pt** | ` 御見積金額 ￥１３,４２０,０００ `（全角数字・前後に空白） |
| 金額末尾記号 | 756 | 407 | 20pt | `.- ` |
| 税注記 | 386 | 452 | **13pt** | `上記見積金額に消費税は含んでおりません。` |
| 有効期限 | 764 | 452 | 10pt | `（見積有効期限：提出日より 1 ヶ月間）` |
| 工事件名 | 125 | 492 | 12pt | `工事件名：JAあかし 農業倉庫改修工事` |
| 工事場所 | 125 | 524 | 12pt | `工事場所：明石市大久保町駅前1丁目2-10` |
| 別途工事 見出し | 129 | 560 | 12pt | `別途工事` |
| 別途工事 ①〜⑤ | 200 | 586 / 618 / 649 / 680 / 714 | **11pt** | `①この見積書に記載なき事項`、②〜⑤は記号のみ（空欄） |

### 自社情報（右側ブロック）

| 要素 | x (px) | y (px) | サイズ | 内容例 |
|---|---|---|---|---|
| 社名 | 646 | 531 | **22pt** | `三 和 建 設 株 式 会 社 `（1文字ずつ全角スペース区切り） |
| 代表者 | 702 | 578 | **14pt** | `代表取締役　　中 農　一 誠` |
| 郵便番号 | 673 | 616 | 10pt | `〒670-0933` |
| 本社住所 | 673 | 639 | 10pt | `本　　社：兵庫県姫路市平野町４３番地` |
| TEL | 748 | 662 | 10pt | `TEL．079-281-5858` |
| FAX | 748 | 680 | 10pt | `FAX．079-281-5978` |

> 別途工事の②〜⑤、自社情報、税注記、有効期限は**固定文言またはマスタ由来**であり、見積書データには存在しない。データ供給元を要件定義で決める（会社情報は `company-info` spec が管理）。

---

## 4. 内訳書・明細書 共通の表組み

内訳書と明細書は**同一の表レイアウト**を共有し、表題文字列のみ異なる。

### 縦罫線（列境界）

| # | 列 | 左端 px | 右端 px | 幅 px | 幅 mm |
|---|---|---|---|---|---|
| 1 | 名称 | 59.8 | 339.7 | 279.9 | 74.1 |
| 2 | 規格 | 339.7 | 547.5 | 207.8 | 55.0 |
| 3 | 単位 | 547.5 | 608.0 | 60.5 | 16.0 |
| 4 | 数量 | 608.0 | 717.6 | 109.6 | 29.0 |
| 5 | 単価 | 717.6 | 827.4 | 109.8 | 29.1 |
| 6 | 金額 | 827.4 | 944.5 | 117.1 | 31.0 |
| 7 | 備考 | 944.5 | 1063.4 | 118.9 | 31.5 |

表全体: x 59.8 – 1063.4 px（幅 1003.6 px = 265.5 mm）。左余白 15.8 mm / 右余白 15.6 mm。

> 現行実装の列幅は `名称60 / 規格40 / 単位15 / 数量20 / 単価25`（mm、備考なし）。**7列構成への変更が必要。**

### 横罫線（行境界）

y = 83.0 / 94.6 / 128.5 / 162.4 / 196.5 / 230.6 / 264.5 / 298.6 / 332.5 / 366.6 / 400.6 / 434.6 / 468.6 / 502.6 / 536.6 / 570.7 / 604.6 / 638.7 / 672.8 / 706.7 / 739.8 px

| 区分 | 範囲 | 高さ |
|---|---|---|
| 表題領域 | 83.0 – 94.6 | — |
| **ヘッダ行** | 94.6 – 128.5 | 33.9 px（9.0 mm） |
| **明細行 × 17** | 128.5 – 706.7 | 34.0 px（9.0 mm）ピッチ |
| **合計行** | 706.7 – 739.8 | 33.1 px |

**1ページあたり 明細17行 ＋ 合計1行 = 18行グリッド。行数は固定で、データが少なくても罫線は最終行まで引かれる。**

線幅は 0.8 px（内側罫線）と 1.76 px（外枠・見出し区切り）の2種。

### テキストのベースライン
各行の上罫線から **+21.5 px** の位置（例: 行境界 128.5 → テキスト y=150）。

### 表題・ページ番号

| 要素 | x (px) | y (px) | サイズ | 内容 |
|---|---|---|---|---|
| 表題 | 508 | 80 | **16pt** | `内  訳  書` / `明  細  書`（全角2スペース区切り） |
| ページ番号 | 78〜80 | 85 | 10pt | `No.` ＋ `Page.N` |

### ヘッダ行（y=116、10pt、各列中央）

| 列 | x (px) | 表記 |
|---|---|---|
| 名称 | 136 | `名               称` |
| 規格 | 400 | `規         格` |
| 単位 | 565 | `単位` |
| 数量 | 641 | `数  量` |
| 単価 | 747 | `単   価` |
| 金額 | 857 | `金    額` |
| 備考 | 976 | `備    考` |

> 見出し文字列は**全角スペースで字間を空けて列幅いっぱいに配置**する表記。

---

## 5. 内訳書（Page 2）固有

- 表題: `内  訳  書`
- 各行は第1階層項目1件に対応

| 列 | x (px) | 内容例 | 揃え |
|---|---|---|---|
| 名称 | 67 | `  Ａ.共通仮設工事`（**先頭に半角2スペース**） | 左 |
| 単位 | 571 | `式`（2行目以降は `〃`） | 中央 |
| 数量 | 674 | `1     ` | 右（後述） |
| 金額 | 874〜888 | `486,304` | 右 |

- **値引行**: 名称 `【値引】`、金額 `-48,585`（マイナス表示。単位・数量は空欄）
- **注記行**: 明細行の途中に名称列だけを使った注記を置ける。例 y=490 `※ﾄｲﾚ・電力100V・水道水無償での御支給をお願いします。`
- **合計行**（y=728）: 名称列 x=67 に `        【合  計】`（**先頭に半角8スペース**）、金額列に総額

---

## 6. 明細書（Page 3以降）固有

- 表題: `明  細  書`
- **1行目に親項目（第1階層）の見出し行**を置く

| 要素 | x (px) | 内容例 |
|---|---|---|
| 記号 | 80 | `Ａ` |
| 区切り | 94 | `.` |
| 名称 | 100 | `共通仮設工事` |

- **2行目以降が子項目**。名称は x=107（見出し行より **+27 px インデント**）
- **合計行**（y=728）: `        【合  計】` ＋ その階層の合計金額
- **フッタ**（y=757、x=944〜957、10pt）: **表の外側下部・右寄せに親項目名を再掲**（例 `  Ａ.共通仮設工事`）

### 明細行の記入例

```
名称 x=107          規格 x=346              単位 x=565-575  数量 x=661-674  単価 x=777-797  金額 x=874-901
保安要員            仮設足場組立・解体時     人工            10              17,500          175,000
外部足場            ｸｻﾋﾞ緊結式               m               612             592             362,304
新設塩ﾋﾞｼｰﾄ防水                              〃              304.6           8,950           2,726,170
```

- **名称のみで規格・単位・数量・単価が空の注記行**を明細中に置ける（例 P7 `庇働き面積182.7㎡、塗装面積(係数1.5倍) 274.1㎡`、`※ﾏｲﾙｰﾌｧｰ MM工法`）

---

## 7. 値の書式ルール

| 項目 | ルール |
|---|---|
| **数量** | 小数第1位まで表示し、**小数点位置を揃える**。整数値は小数部を空白で埋める（`1     ` / `612     ` / `343.1   ` / `53.7   `） |
| **単価・金額** | 3桁カンマ区切り、小数なし、右揃え。ゼロ・未設定は空欄 |
| **マイナス金額** | 先頭に `-`（`-48,585`） |
| **単位の繰り返し** | 直前行と同じ単位は `〃` に置換 |
| **表紙の金額** | **全角数字**＋全角カンマ（`￥１３,４２０,０００`）、末尾に `.- ` |
| **表紙の日付** | 全角数字（`２０２６ 年５ 月１ 日`） |
| **階層記号** | 全角英大文字 `Ａ` `Ｂ` `Ｃ` …（内訳書・明細書とも共通） |

---

## 8. フォントサイズ一覧（実寸pt）

| 用途 | pt |
|---|---|
| 表紙 表題「御見積書」 | 25 |
| 表紙 社名 | 22 |
| 表紙 見積金額 | 20 |
| 内訳書/明細書 表題 | 16 |
| 表紙 宛先・代表者 | 14 |
| 表紙 税注記 | 13 |
| 表紙 リード文・工事件名・工事場所・別途工事 | 12 |
| 表紙 別途工事①〜⑤ | 11 |
| **表の本文・ヘッダ・ページ番号・自社連絡先** | **10** |

---

## 9. 参照PDF側の不備（**再現しないこと**）

- Page 5 に、ページ幅（1063.4 px）を超える x=1164 / x=1340 の位置へ 41pt でテキストが配置されている（`機械的固定工法 土間部 8Φﾋﾞｽ使用`、`機械的固定工法 立上部 ﾊﾟﾗﾍﾟｯﾄ下部まで`）。**備考列に収まらず紙面外へはみ出したもの**で、書式の意図ではない。
- 実装では備考列に収まらないテキストの**折り返しまたは切り詰め**を行う。方針は要件定義で決める。

---

## 10. 実装方式：フロントエンド生成へ移行する

### 結論
**現場調査報告書・数量表と同じ「フロントエンドで jsPDF を使う」構成を採用する。** 目標書式の実現に必要な部品はすべてフロントエンドに既に存在しており、新規ライブラリの導入もフォント資産の追加も不要。

### 決定的な理由：日本語フォント資産はフロントエンドにしか無い

| | フロントエンド | バックエンド |
|---|---|---|
| jsPDF | `^4.0.0` | `^4.1.0` |
| **日本語フォント** | **Noto Sans JP を埋め込み済み** | **なし** |
| フォント資産 | `frontend/src/services/export/fonts/noto-sans-jp-base64.ts`<br>base64 3,007,752文字 → バイナリ **2,255,812 bytes**<br>先頭 `\x00\x01\x00\x00` = 実体のある TrueType | 存在しない |
| 登録処理 | `PdfFontService.initialize()` が `addFileToVFS` → `addFont` → `setFont`（`PdfFontService.ts:120-126`） | `addFont` / `addFileToVFS` の呼び出しなし |

> ソース中のコメントは「サブセット化（約500KB）」と記載しているが、実測は 2.25 MB のフルセット。**記述と実体が食い違っている**ため、サブセット化の余地がある（`PdfFontService.ts:6, 17`）。

現行の見積書PDFは**バックエンド生成**（`EstimateExportDialog.tsx:274` → `GET /api/estimates/:id/export`、`estimates.routes.ts:2068`）であり、日本語フォントを持たない側で生成している。これが日本語を出力できない直接の原因。

### 再利用する既存サービス

| サービス | 提供機能 | 転用内容 |
|---|---|---|
| `frontend/src/services/export/PdfFontService.ts` | Noto Sans JP の jsPDF 登録。`PDF_FONT_FAMILY = 'NotoSansJP'`、`PDF_FONT_NAME = 'NotoSansJP-Regular.ttf'`。シングルトン＋`FontLoadStatus` 管理 | **そのまま利用** |
| `frontend/src/services/export/QuantityTablePdfExportService.ts` | `TABLE_COLUMNS` 定義（`:92`）、`TABLE_ROW_HEIGHT`（`:82`）、`doc.rect`（`:240, :302`）・`doc.line`（`:316, :368`）による**罫線描画**、改ページ判定（`:280-281`） | **罫線グリッドの実装前例**。列定義を本書 §4 の7列へ差し替える |
| `frontend/src/services/export/PdfReportService.ts` | `renderCoverPage`（`:248`）/ `renderInfoSection`（`:347`）/ `renderImagesSection`（`:402`）によるページ単位レンダリング | **表紙ページの実装前例** |
| `frontend/src/services/export/PdfExportService.ts` | `downloadPdf` / `generateDefaultFilename` / `PdfExportProgress` による進捗通知 | **ダウンロード共通処理として利用** |

呼び出し元の前例: `SiteSurveyDetailInfo.tsx:323`（現場調査報告書）、`QuantityTableEditPage.tsx:40, 43`（数量表）、`EstimateRequestDetailPage.tsx:57`（見積依頼）。

### 構成変更

```
変更前: EstimateExportDialog → GET /api/estimates/:id/export
                              → backend estimate-export.service.ts（jsPDF、フォントなし）

変更後: EstimateExportDialog → frontend EstimatePdfExportService（新規）
                              → PdfFontService でフォント登録
                              → QuantityTablePdfExportService と同方式で罫線描画
                              → PdfExportService.downloadPdf でダウンロード
```

### 付随して得られる利点
- フォント 2.25 MB をバックエンドへ複製する必要がない（フロントの増分はゼロ。既存3機能と同じチャンクに載る）
- PDF生成がサーバーから外れる。本改訂全体の「サーバーリクエスト削減」方針と整合する
- **未保存状態のプレビューが可能になる**。編集中のクライアント state から直接生成できる（バックエンド生成では保存済みデータしか出力できない）

### 移行時の判断事項
- **Excel出力の扱い**: `GET /:id/export` は `format=pdf|xlsx` の両対応。PDFのみ移すと出力経路が2系統に分かれる。Excelも移すかバックエンドに残すか決める。
- **バックエンド側の後始末**: `estimate-export.service.ts` の PDF 生成部が不要になる — `exportToPdf`（`:165`）、`exportToPdfWithLineTypes`（`:406`）、`generateCoverPage`（`:444`）、`generateSummaryPage`（`:477`）、`generateDetailPages`（`:514`）、`drawTableHeader`（`:558`）、`drawTableRow`（`:583`）およびその単体テスト。
- **フォント失敗時の挙動**: `PdfReportService` はフォント登録失敗時に helvetica へフォールバックする（`:725-728`, `:790-794`）。見積書は**日本語が文字化けしたPDFを出すべきではない**ため、フォールバックせずエラーとして中断する方針を検討する。

---

## 11. 現行実装とのギャップ

対象: `backend/src/services/estimate-export.service.ts`（jsPDF 4.1.0 使用）。§10 の方針によりフロントエンドへ移行するため、以下は**移行先で満たすべき差分**として読む。

| # | 項目 | 現行 | 目標 |
|---|---|---|---|
| 1 | **日本語フォント** | **未埋め込み**（`addFont` / `addFileToVFS` の呼び出しが無く、リポジトリにフォント資産も無い）。jsPDF 標準フォントは WinAnsi のみのため**日本語が出力できない** | フロントエンドの `PdfFontService` を利用して解決（§10） |
| 2 | 用紙向き | `portrait`（A4縦、`:173`） | **landscape**（A4横） |
| 3 | 列構成 | 名称/規格/単位/数量/単価 の5列（`:100-105`）、備考・金額の幅定義なし | **7列**（名称/規格/単位/数量/単価/金額/備考） |
| 4 | 罫線 | **描画なし**（`doc.text` のみ） | 縦7本・横21本の罫線グリッド、線幅2種 |
| 5 | 行グリッド | 可変 | **17明細行＋合計行の固定18行**。データが少なくても罫線を引く |
| 6 | ページ構成 | 表紙 → 項目一覧 → 階層ごとの詳細（`:179-186`） | 表紙 → **内訳書** → **子を持つ第1階層ごとの明細書** |
| 7 | 表紙 | 表題・プロジェクト名・見積名・日付・合計のみ（`:444-472`） | 宛先・網掛け表題・全角金額・工事件名/場所・別途工事①〜⑤・自社情報ブロック |
| 8 | 合計行 | なし | 各ページ最下部に `        【合  計】` |
| 9 | フッタ | なし | 明細書ページ右下に親項目名を再掲 |
| 10 | 数量書式 | `line.quantity?.toString()`（`:596`） | 小数第1位で**小数点位置揃え** |
| 11 | 単位の `〃` 置換 | なし | 直前行と同一なら `〃` |
| 12 | 値引行 | なし | `【値引】` 行、マイナス表示 |
| 13 | 注記行 | なし | 名称列のみを使う注記行 |
| 14 | 文字切り詰め | `truncateText(name, 20)` / `(spec, 15)`（`:587-591`）で固定文字数切り詰め | 列幅に応じた折り返しまたは切り詰め |

### 解決済みの論点
- **日本語フォントの選定と配布方法** → フロントエンドに Noto Sans JP が埋め込み済み（`PdfFontService`）。新規調達・ライセンス検討は不要。
- **PDF生成ライブラリの継続可否** → jsPDF を継続する。罫線グリッドは `QuantityTablePdfExportService` に実装前例があり、HTMLテンプレート＋ヘッドレスブラウザへの移行は不要。

### 要件定義で決める論点
1. **明細が17行を超える場合**の継続ページの扱い（合計行の出し方、`Page.N` の連番、フッタの親項目名）。
2. **第3階層以降**の表現。参照PDFは2階層（第1階層＝内訳書の行、第2階層＝明細書の行）のみで実例が無い。
3. **表紙の固定文言・自社情報のデータ供給元**（`company-info` spec との連携）。
4. **別途工事①〜⑤・注記行・有効期限**の入力手段。現在の見積データモデルに対応するフィールドが無い。
5. **既存の行タイプ選択機能（REQ-32）との整合**。参照PDFは見積金額行のみの単一表。実行金額行・業者金額行を出力する場合の書式。
6. **Excel出力を同時にフロントエンドへ移すか**、バックエンドに残して出力経路を2系統にするか。
7. **フォント登録失敗時にフォールバックするか中断するか**（現行 `PdfReportService` は helvetica へフォールバックする）。
8. **フォントのサブセット化**を行うか。コメント記載の 500KB に対し実体は 2.25 MB。既存3機能と共有する資産のため、変更は他機能へも影響する。
9. **未保存状態でのプレビュー出力**を提供するか。フロントエンド生成なら技術的に可能になる。
